import { AdaptiveBackgroundDirective } from './adaptive-background.directive';

describe('AdaptiveBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new AdaptiveBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
